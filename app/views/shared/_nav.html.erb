<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Navbar", tasks_path, class:"navbar-brand" %>
    <%= search_form_for @q, class:"d-flex" do |f| %>
      <%= f.search_field :name_cont, class:"form-control me-2", placeholder:"輸入任務名稱" %>
      <%= f.submit "Search",class:"btn btn-outline-success"  %>
    <% end %>
    <div class="d-flex justify-content-end">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="#">Hi! <%= current_user.username %></a>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            會員
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <li><%= link_to "新增任務", new_task_path, class: "dropdown-item" %></li>
              <li><%= link_to "登出", sign_out_path, method: 'delete', class: "dropdown-item"%></li>
            <% else %>
              <%= link_to "註冊", sign_up_path, class: "dropdown-item" %>
              <%= link_to "登入", sign_in_path, class: "dropdown-item" %>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>